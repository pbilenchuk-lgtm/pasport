state.json
.state-*.json

# Списки прокси содержат логины и пароли — в репозиторий им нельзя.
proxies.txt
proxy*.txt
Proxy*.txt

__pycache__/
*.py[cod]
.venv/
venv/

# Токен и chat_id хранятся здесь — в репозиторий им нельзя.
.env

# Логи фонового запуска.
monitor.log
monitor.log.*

*.har
debug-*.html
