# Зависимости для разработки и тестов.
-r requirements.txt

# Статический анализ. Ловит неопределённые имена — тот класс ошибок, который
# обычные тесты пропускают, если ветка кода не выполняется.
pyflakes==3.2.0

pytest==8.3.3
