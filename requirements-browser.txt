# Дополнительные зависимости для Варианта Б (headless-браузер).
# Ставятся поверх requirements.txt, нужны только при FETCH_MODE=browser или auto.
-r requirements.txt
-r requirements-impersonate.txt

playwright==1.47.0

# Маскирует признаки автоматизации. Версия 2.x свежее и прячет больше, чем 1.x;
# fetcher.py умеет оба API, так что откат на 1.0.6 тоже рабочий.
playwright-stealth==2.0.3
