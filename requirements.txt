# Базовые зависимости (Вариант А — прямые HTTP-запросы).
#
# Здесь только пакеты на чистом Python: они ставятся на любой версии, включая
# самые свежие, и не требуют компилятора. Это важно для установки на домашний
# Windows-компьютер, где собирать бинарные пакеты нечем.
#
# Границы версий нижние, а не точные: пусть pip возьмёт свежую сборку,
# совместимую с установленным Python.

beautifulsoup4>=4.12

# HTTP-клиент с поддержкой HTTP/2.
httpx[http2]>=0.27

# База часовых поясов. В минимальных образах (включая официальный образ
# Playwright) её нет, и ZoneInfo("Europe/Warsaw") не находит зону.
tzdata>=2024.1
