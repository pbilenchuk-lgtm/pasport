# Базовые зависимости (Вариант А — прямые HTTP-запросы).
beautifulsoup4==4.12.3

# HTTP-клиент с HTTP/2.
httpx[http2]==0.27.2

# Подделывает TLS-отпечаток Chrome. Не обязателен, но заметно повышает шанс
# пройти Cloudflare: fetcher.py использует его автоматически, если он есть.
curl_cffi==0.7.4
