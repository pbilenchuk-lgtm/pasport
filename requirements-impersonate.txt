# Необязательное дополнение: подделка TLS-отпечатка Chrome.
#
# Нужно только там, где Cloudflare придирается к прямым запросам — например на
# сервере с датацентровым IP. Дома, где сайт открывается напрямую, смысла нет.
#
# Пакет бинарный, поэтому под совсем свежие версии Python сборки может ещё не
# быть. Это не проблема: fetcher.py подхватывает его автоматически, если он
# установлен, и спокойно работает через httpx, если нет.

curl_cffi>=0.7
